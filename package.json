{
  "name": "pg-migrate-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "yarn build && node ./build/app.js"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postgres-migrations": "^3.0.2",
    "typescript": "^3.4.5"
  }
}
